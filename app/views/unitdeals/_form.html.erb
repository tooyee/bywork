<%= form_with(model: unitdeal, local: true) do |form| %>
  <% if unitdeal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unitdeal.errors.count, "error") %> prohibited this unitdeal from being saved:</h2>

      <ul>
      <% unitdeal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code, id: :unitdeal_code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :unitdeal_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
