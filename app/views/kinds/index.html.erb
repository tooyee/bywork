<p id="notice"><%= notice %></p>

<h1>Kinds</h1>

<table>
  <thead>
    <tr>
      <th>Casework</th>
        <th>Id</th>
      <th>Code</th>
      <th>Name</th>
      <th>Remarks</th>
      <th>Kindtype</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @kinds.each do |kind| %>
      <tr>
        <td><%= kind.casework.name %></td>
        <td><%= kind.id %></td>
        <td><%= kind.code %></td>
        <td><%= kind.name %></td>
        <td><%= kind.remarks %></td>
        <td><%= kind.kindtype %></td>
        <td><%= kind.status %></td>
        <td><%= link_to 'Show', kind %></td>
        <td><%= link_to 'Edit', edit_kind_path(kind) %></td>
        <td><%= link_to 'Destroy', kind, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Kind', new_kind_path %>
